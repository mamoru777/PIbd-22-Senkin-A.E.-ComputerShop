@using ComputerShopContracts.ViewModels
@model List<ZakupkaViewModel>
@{
    ViewData["Title"] = "Home Page";
}
<div class="text-center">
    <h1 class="display-4">Закупки</h1>
</div>
<div class="text-center">
    @{
        if (Model == null)
        {
            <h3 class="display-4">Авторизируйтесь</h3>
            return;
        }
        <p>
            <a asp-action="CreateZakupka">Создать закупку</a>
        </p>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Номер
                    </th>
                    <th>
                        Закупка
                    </th>
                    <th>
                        Дата закупки
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Id)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ZakupkaName)

                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.DateBuy)
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>